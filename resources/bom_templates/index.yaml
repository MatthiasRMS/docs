tags:
  - bom_template
summary: Fetch all bom templates
description: Fetch all the BOM templates belonging to a company
responses:
  "200":
    description: Successful operation
    content:
      application/json:
        schema:
          type: array
          items:
            $ref: "./schema.yaml#/components/schemas/BomTemplateResponse"
  "405":
    description: Invalid input
