tags:
  - measurement_template
summary: Delete a measurement template
description: ""
parameters:
  - name: id
    in: path
    description: Measurement template id to delete
    required: true
    schema:
      type: string
      format: uuid
responses:
  "400":
    description: Invalid measurement template
