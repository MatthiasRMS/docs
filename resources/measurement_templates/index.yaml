tags:
  - measurement_template
summary: Fetch all measurement templates
description: Fetch all the measurement templates belonging to a company
responses:
  "200":
    description: Successful operation
    content:
      application/json:
        schema:
          type: array
          items:
            $ref: "./schema.yaml#/components/schemas/MeasurementTemplateResponse"
  "405":
    description: Invalid input
