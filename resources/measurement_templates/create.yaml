tags:
  - measurement_template
summary: Add a new measurement template
description: Add a new measurement template
responses:
  "200":
    description: Successful operation
    content:
      application/json:
        schema:
          $ref: "./schema.yaml#/components/schemas/MeasurementTemplateResponse"
  "405":
    description: Invalid input
requestBody:
  description: Create a new measurement template
  required: true
  content:
    application/json:
      schema:
        $ref: "./schema.yaml#/components/schemas/MeasurementTemplateBase"
