tags:
  - color
summary: Fetch all colors
description: Fetch all colors belonging to a company
responses:
  "200":
    description: Successful operation
    content:
      application/json:
        schema:
          type: array
          items:
            $ref: "./schema.yaml#/components/schemas/ColorResponse"
  "405":
    description: Invalid input
