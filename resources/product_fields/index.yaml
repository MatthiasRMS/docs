tags:
  - product_field
summary: Fetch all products fields
description: Fetch all the products fields belonging to a company
responses:
  "200":
    description: Successful operation
    content:
      application/json:
        schema:
          type: array
          items:
            $ref: "./schema.yaml#/components/schemas/ProductFieldResponse"
  "405":
    description: Invalid input
