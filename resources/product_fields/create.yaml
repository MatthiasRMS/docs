tags:
  - product_field
summary: Add a new product field
description: Add a new product field
responses:
  "200":
    description: Successful operation
    content:
      application/json:
        schema:
          $ref: "./schema.yaml#/components/schemas/ProductFieldResponse"
  "405":
    description: Invalid input
requestBody:
  description: Create a new product field
  required: true
  content:
    application/json:
      schema:
        $ref: "./schema.yaml#/components/schemas/ProductField"
