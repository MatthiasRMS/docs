tags:
  - quality_control
summary: Add a new quality control
description: Add a new quality control
responses:
  "200":
    description: Successful operation
    content:
      application/json:
        schema:
          $ref: "./schema.yaml#/components/schemas/QualityControlResponse"
  "405":
    description: Invalid input
requestBody:
  description: Create a new quality control
  required: true
  content:
    application/json:
      schema:
        $ref: "./schema.yaml#/components/schemas/QualityControlBase"
